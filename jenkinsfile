pipeline {
  agent any
  stages {
    stage('Maven Build') {
      steps {
        sh 'echo Jenkinsfile demo'
      }
    }
  }
  post {
  success {
    This is success block.
  }
  failure {
    This is failure block.
    }
  }
}
